const std = @import("std");
const log = std.log;

pub fn solve(allocator: std.mem.Allocator) !void {
    //const content = @embedFile("./data/day04.txt");

    std.debug.print("Part 1 -> {d}\n", .{0});
    std.debug.print("Part 2 -> {d}", .{0});
}
